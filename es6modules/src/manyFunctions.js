const multiplyNumbers = (a,b) => {
    return a * b;
}

const divideNumbers = (a,b) => {
    return a / b;
}

// Export two functions using ES6 module syntax.
export { multiplyNumbers, divideNumbers };
